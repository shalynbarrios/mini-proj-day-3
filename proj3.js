function setup() {
  createCanvas(600, 600);
    background(100);
}

function draw() {
  // below
  noStroke(0);
  fill(200, 0, 100)
  ellipse(mouseX, mouseY, 75, 120)
}

function mousePressed() {
  background(100);
}